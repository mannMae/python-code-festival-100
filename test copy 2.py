class Solution:
    def largestRectangleArea(self, heights: list[int]) -> int:
        height = 0
        maxWidth = 0
        while sum(heights):
            height +=1
            for i, j in enumerate(heights):
                heights
                maxWidth = max(maxWidth, )
                if i:
                    heights[j] -=1
        return maxWidth

a = Solution

a.largestRectangleArea([1,2,3])